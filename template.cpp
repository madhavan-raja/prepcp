#include <bits/stdc++.h>
using namespace std;

#define endl '\n'

#define ll long long int
#define ull unsigned ll

#define vt vector
#define pb push_back

#define F(i, end) LF(i, 0, end)
#define LF(i, beg, end) for (ll i = beg; i < end; ++i)
#define LR(i, end, beg) for (ll i = end; i >= beg; --i)

#define outvec(vec, i, sep) F(i, vec.size()) cout << vec[i] << sep

void solve()
{
    //
}

int main()
{
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL);
    cout.tie(NULL);
    
    ll c = 1;

    cin >> c;

    F(i, c)
        solve();
    
	return 0;
}
